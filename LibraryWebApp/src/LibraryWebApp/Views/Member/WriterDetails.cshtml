@model LibraryWebApp.Models.Writer

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<h2 style="margin-top: 100px; text-align:center; background-color:antiquewhite; margin-left:30%; margin-right:30%">Writer</h2>

<div style="text-align:center; margin-left:30%; margin-right:30%">
    @if (Model != null)
    {
        <div class="thumbnail">
           
            <div class="caption">
                <h3 style="margin-top: -5px;text-align:center;">
                    @Model.LastName, @Model.FirstName (@Model.Birthday.ToShortDateString())
                </h3>
                <h4>Djela:</h4>
                @foreach (var book in Model.WritersBooks)
                {
                    <a asp-action="BookDetails" asp-controller="Member" asp-route-id="@book.BookId">@book.Title</a>

                }

            </div>
            @*<p>

                    Naslov: @Model.Title
                    <br>
                    Autor: @Model.Writer.FirstName @Model.Writer.LastName<br>
                    Genre: @Model.Genre<br>
                    About:<br>
                    <h5 style="margin-left: 30px">
                        @Model.About<br>
                    </h5>
                    Kolicina: @Model.Counter<br>

                    <input type="submit" value="Posudi knjigu" class="btn btn-primary btn-lg" onclick="location.href='@Url.Action("BorrowBook", "Member",new { id = @Model.BookId })'" />

                </p>*@


            @*@Html.ActionLink("Edit", "Index", new { /* id = Model.PrimaryKey */ }) |*@
            <a asp-action="AllWriters">Back to List</a>
        </div>
    }
</div>