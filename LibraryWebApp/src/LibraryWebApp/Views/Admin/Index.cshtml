@model List<Book>

<!DOCTYPE html>

<html>
<head>
    <title>Admin Acc</title>
</head>
<body>
<div>
    <h3 style="text-align: center">
        <a asp-action="Add">Add new Book!</a>
    </h3>
    

    @if (Model != null)
    {
        <h1>Books:</h1>
        foreach (var item in Model)
        {
            <p>Ime knjige: @item.Title Pisac: @item.Writer.FirstName @item.Writer.LastName Kolicina za posudivanje: @item.BorrowCounter Kolicina za kupovanje: @item.SaleCounter
            <a asp-controller="Admin" asp-action="DeleteBook" asp-route-id="@item.BookId">Delete Book</a>
            <a asp-controller="Admin" asp-action="Edit" asp-route-id="@item.BookId">Edit Book</a></p>

        }
    }

</div>
</body>
</html>